{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Song creation</h1>

    <form action="{{ path('song_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('song') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
