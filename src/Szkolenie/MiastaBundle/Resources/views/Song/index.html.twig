{% extends '::base.html.twig' %}

{% block body -%}

    <ul>
        {% for entity in entities %}
            <li><a href="{{ path('song_show', {'id': entity.id}) }}">{{ entity.title }} </a></a></li>
        {% endfor %}
    </ul>

{% endblock %}
